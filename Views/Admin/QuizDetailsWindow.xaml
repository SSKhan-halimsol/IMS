<Window x:Class="IMS.Views.Admin.QuizDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quiz Details"
        Height="250" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#1E1E2E"
        Foreground="White">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Quiz Summary" 
                       FontSize="20"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            <TextBlock x:Name="ScoreText" FontSize="16" Margin="0,5"/>
            <TextBlock x:Name="PassedText" FontSize="16" Margin="0,5"/>
            <TextBlock x:Name="DateText" FontSize="16" Margin="0,5"/>
            <Button Content="Close"
                    Width="80"
                    Height="30"
                    Background="#4F46E5"
                    Foreground="White"
                    FontWeight="Bold"
                    HorizontalAlignment="Center"
                    Margin="10"
                    Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>